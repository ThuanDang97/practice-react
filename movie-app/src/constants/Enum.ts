enum DefaultRangerValue {
  Min = 0,
  Max = 10,
  DefaultValue = 10,
  Step = 1,
}

enum MovieType {
  NowPlaying = 'now-playing',
  UpComing = 'up-coming',
  TopRated = 'top-rated',
  Favorite = 'favorite-movies',
  Popular = 'popular',
}

export { DefaultRangerValue, MovieType };
